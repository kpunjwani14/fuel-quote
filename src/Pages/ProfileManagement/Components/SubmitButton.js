import React from "react";
import { Button } from 'react-bootstrap';

function SubmitButton() {
    return (
        <>
            <Button style={{ float: 'right' }} size="lg" type="submit">Save</Button>
        </>
    )
};

export default SubmitButton;